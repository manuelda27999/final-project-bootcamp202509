export default function Footer() {
  return (
    <footer>
      {/* Mensaje legal que se repite en todas las páginas */}
      <small>© {new Date().getFullYear()} Conecta Team — Tu red social corporativa</small>
    </footer>
  );
}
